export interface Pokemon {
  id: number;          // Identifiant du Pokémon
  name: string;        // Nom du Pokémon
  image: string;       // URL de l'image
  types: string[];     // Types du Pokémon (ex: feu, eau)
}
